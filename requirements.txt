# Streamlit Community Cloud installs from this file, not from pyproject.toml.
#
# The dashboard is deliberately a standalone consumer: it imports no project
# code, so this is a short list rather than the whole pipeline's dependency
# set. Deploying a Spark job to a dashboard host would be absurd.
streamlit>=1.39,<2
snowflake-connector-python[pandas]>=3.12,<4
pandas>=2.2,<3
# For local runs. On Streamlit Cloud there is no .env and load_dotenv is a no-op.
python-dotenv>=1.0,<2
# Line movement needs real zoom: the series run to hundreds of points over
# days and the interesting parts are minutes wide.
plotly>=5.24,<7
