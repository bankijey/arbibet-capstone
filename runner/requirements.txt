# The runner image's dependencies (runner/Dockerfile). Pinned to the versions
# the DuckDB migration was tested with in September 2026.
duckdb==1.5.5
dbt-core==1.12.3
dbt-duckdb==1.11.0
pyspark==4.2.0
psycopg[binary]==3.3.5
pandas==2.2.3
numpy==2.1.2
openai==3.6.0
httpx==0.28.1
python-dotenv==1.2.3
# Spark hands results to pandas through Arrow; without it the fallback is slow.
pyarrow==21.0.0
